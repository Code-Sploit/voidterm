#include <iostream>

using namespace std;

// DEFINE CONSTANT FALLBACK VARIABLES:

const QString colorScheme = "Linux";

const float opacity = 0.0;

const int fontsize = 10;

const bool boldfont = false;
const bool italicfont = false;

const string fonttype = "Monospace";
const string defaultshell = "/bin/bash";
const string editor = "/usr/bin/vim";
const string term = "konsole-256color";
